import React from 'react'

var StepNavigator = React.createClass({
    render: function() {
        return (
            <div className='step-navigator'>Step Navigator</div>
        )
    }
});

export default StepNavigator
